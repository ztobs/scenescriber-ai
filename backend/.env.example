# SceneScriber AI - Environment Variables Example
# Copy this file to .env and fill in your API keys

# ======================
# 🔐 AI Provider API Keys
# ======================

# OpenAI (for GPT-4 Vision)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI-compatible provider (e.g., Azure OpenAI, local LLM server)
# Leave empty to use standard OpenAI API
OPENAI_COMPATIBLE_API_BASE=
OPENAI_COMPATIBLE_API_KEY=
# Optional: Custom API version (for Azure OpenAI)
OPENAI_COMPATIBLE_API_VERSION=2024-02-15-preview

# Anthropic (for Claude 3)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google AI (for Gemini)
# Get from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# ======================
# ⚙️ Application Settings
# ======================

# Single-image models: Models that only support 1 image per request
# These models will have their keyframes stitched into a single horizontal image
# Add model names (comma-separated, case-insensitive) that need this feature
# 
# Known single-image models:
# - llama3.2-vision, llama3.2:11b-vision, llama3.2:90b-vision (Meta)
# - gpt-4-vision-preview (early GPT-4 Vision, deprecated)
# - moondream, moondream2 (Moondream Vision)
# - cogvlm, cogvlm2 (THUDM CogVLM)
# - minicpm-v, minicpm-v-2 (OpenBMB MiniCPM-V - depends on version)
# 
# Known multi-image models (DO NOT add these):
# - gpt-4o, gpt-4o-mini (OpenAI - full multi-image support)
# - claude-3-opus, claude-3-sonnet, claude-3-haiku (Anthropic)
# - gemini-pro-vision, gemini-flash (Google)
# - qwen-vl-plus, qwen-vl-max (Alibaba Qwen-VL)
# - pixtral-12b (Mistral AI)
# 
# To add your own model to this list, just append its name (e.g., "my-custom-model")
SINGLE_IMAGE_MODELS=llama3.2-vision,llama3.2:11b-vision,llama3.2:90b-vision,llama3.2,gpt-4-vision-preview,moondream,moondream2,cogvlm,cogvlm2,minicpm-v

# Processing settings
DEFAULT_SENSITIVITY=medium
DEFAULT_MIN_SCENE_DURATION=2.0
DEFAULT_AI_MODEL=openai  # openai, claude, gemini, llava
DEFAULT_DESCRIPTION_LENGTH=medium

# Logging
LOG_LEVEL=INFO

# ======================
# 🔒 Security
# ======================

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ======================
# 🧹 Cleanup Settings
# ======================

# Cleanup retention days for logs and generated files
# Files older than this many days will be automatically cleaned up
CLEANUP_RETENTION_DAYS=7

# Enable automatic cleanup before new uploads
ENABLE_AUTO_CLEANUP=true

# Directories to clean up (comma-separated)
CLEANUP_DIRECTORIES=uploads,exports,keyframes

# Log files to clean up (comma-separated)
CLEANUP_LOG_FILES=scenescriber.log,backend.log,server.log